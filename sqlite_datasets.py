"""  This module contains functions for interacting with user SQLite datasets.

  Each file that is uploaded to CIIPro should contain two columns: (1) an binarized activity and (2) a native chemical
  chemical identifier (CID, CAS, IUPAC name, SMILES, or InChiKey).

  

  """